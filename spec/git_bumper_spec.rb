require 'spec_helper'

describe GitBumper do
  it 'has a version number' do
    expect(GitBumper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
